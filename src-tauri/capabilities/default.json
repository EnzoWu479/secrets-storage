{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window without privileged commands during the foundation phase",
  "windows": ["main"],
  "permissions": ["core:app:allow-version"]
}
